# Claim Boundary (One Page)

Allowed:

- Raw edge-bit density cannot alone support a universal explicit-RCSP lower
  bound (`C-T5`, `C-T6`).
- A parallel-branch subclass has multiple-marked query complexity under counted
  access to an explicit length-K attribute array (`C-T7`).
- Random-subset membership-only success is bounded by the reviewed hard-cap and
  posterior/advice formulas (`C-T1`, `C-T2`, `C-T8`, `C-T9`).
- One-shot finite-dimensional quantum advice and effective support are stated
  only in their proved models (`C-T10`, `C-T11`).
- Held-out CVaR evidence is specific to Penalty-X p=3 exact-statevector RCSP
  tasks (`C-E5-H1`, `C-E5-H2`).
- Phase 3 is heterogeneous/resource-censored (`C-E6-SCALING`).

Excluded:

- general quantum advantage;
- universal raw-density explicit-RCSP hardness;
- explicit-RAM or compact-input interpretation of T7;
- CVaR breaking a membership-only theorem;
- advice/effective bits as runtime;
- free structured mixers/state preparation;
- confirmed global scaling law;
- refreshed/interactive quantum advice;
- theorem or integration priority while the search remains unresolved.

Audit label: `SECOND_PASS_MACHINE_AUDIT`. Independent human review is pending.
