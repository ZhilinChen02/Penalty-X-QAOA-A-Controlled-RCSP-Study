\section{Problem, Representations, and Information-Access Models}

\paragraph{Purpose.}
Define RCSP tasks, route outputs, edge-bit states, membership queries, explicit
attribute-array queries, and rich Hamiltonian cost access as different models.

\paragraph{Claim IDs.}
\texttt{C-T5, C-T6, C-T7, C-LIM-MODELS, C-E0-UNIVERSE}.

\paragraph{Source asset IDs.}
\texttt{A0036, A0026, A0050, A0065}.

\paragraph{Paragraph outline.}
\begin{enumerate}
  \item Formal explicit RCSP input/output and controlled DAG family.
  \item Edge-bit encoding and definitions of $\phi_{\rm state}$ and
  $\phi_{\rm path}$.
  \item Membership-only random-subset prior.
  \item Explicit length-$K$ random-access attribute model.
  \item Rich energy samples used by O0/O1/O3; classify CVaR accordingly.
\end{enumerate}

\paragraph{Placeholders.}
[F1 three-domain access schematic] [T2 controlled-design contract].

\paragraph{Unsupported-wording warning.}
Never transfer a black-box membership bound directly to explicit rich-energy
RCSP runtime.
